# S4 — Known Exception Reconciliation

**Historical envelope:** 8 failed + 4 errors — expected UNCHANGED.

**S4 changes:** Additive security layer; no historical test IDs removed.

**Legacy paths preserved:** API-key-only connector save without session → NULL-org global row.

**Status:** UNCHANGED (pending full regression verification).
