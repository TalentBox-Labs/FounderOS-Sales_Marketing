# S4 — Integration Audit Provenance Contract

n8n mutations stamp `AgentActionLog.organization_id` via `stamp_agent_action_log_organization` when org binding resolves.

Secrets redacted from audit payload (`_redact_payload`).

No second audit SoT.
